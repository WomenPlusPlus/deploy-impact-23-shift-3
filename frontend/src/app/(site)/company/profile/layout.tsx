export const metadata = {
  title: "SHIFT - Candidate - profile",
  description: "Shift_Enter App - Company - Profile",
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
