export const metadata = {
  title: "SHIFT - Associations - profile",
  description: "Shift_Enter App - Association - Profile",
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
